You are the FRESH P4C coding implementer, gpt-5.6-sol xhigh. No nested agents, commits, pushes or next-phase execution. Base is accepted P4B branch commit 1a4a0f927f167d9f46cef1ee8dc0bf8073a4e418. Read docs/research/view_risk_protocol.md, implementation_status.md (current top governs historical entries), evidence/phase4/p4b-acceptance.json and P4B review8. All 261 research tests currently pass. P4C is the remaining software task before G4; P5 real experiments are OUT OF SCOPE. Do not reopen accepted architecture/protocol or invent another framework.

P4C scope and acceptance:
1. A self-contained, deterministic, CPU-feasible synthetic smoke entrypoint through the package CLI, usable without downloading weights or private data. Reuse accepted P1-P4B APIs and tiny injected models. Exercise real data flow: named-view synthetic images -> deterministic parent perturbations/masks -> verified frozen features/current targets -> candidate and representative learned/scalar/DS controls -> confidence metrics/evaluation. Cover all 15 nonempty masks (including singleton), clean and representative permitted/held-out/common-mode stresses, both fusion trees where relevant, signed effect cases, fixed classifier predictions and regenerated targets. Do not replicate scientific math or just return canned metric values. Reuse actual training/inference/artifact interfaces for a small fit/reload path; do not merely call pytest from a CLI or import test files at runtime. It need not perform the full patient pilot/control study. Explicitly declare exactly which methods/panels are exercised and which are not. Synthetic statuses must never become real patient readiness, a pilot go decision or an empirical benefit claim. Do not bypass frozen production configurations just to make the smoke pass; low-level synthetic helpers may retain their already accepted flexibility.
2. Bounded workspace/output handling: generated synthetic manifests/caches/checkpoints stay in a task-owned temp or requested external/git-ignored directory, validate final/staging paths, clean temporary resources on success/failure, preserve pre-existing user files. Aggregate JSON reports identify software-only mode and unavailable scientific conclusions. No patient data or locked outcomes opened, no network/download required. Make reproducibility defined over scientific outputs; timing fields naturally vary.
3. Resource preflight: read-only CPU/Python/dependency/device/disk/local public-resource availability as practical. Missing CUDA/weights/data/mapping reported as unavailable/not audited, not silently treated ready. No network, eager legacy split readers, real image decoding, private data content audits or real readiness generation. Scope this as software/resource preflight; P5 owns actual inventory and training resources. Avoid exposing private paths/identifiers in public aggregate reports. No download/install action in preflight.
4. Cost measurement support/procedure: runnable warmup + repeated timing, explicit device synchronization where needed, batch/input shape/device/library/hardware context, honest units and median/spread; separate encoder/feature validation/cache I/O, head/control and end-to-end costs when measured. Report parameter counts and cache byte counts where actual; label estimates versus measurements. Memory fields must accurately name what is measured (e.g. CUDA allocated peak vs CPU process high-water RSS, not Python allocations called tensor memory). No synthetic timing presented as CLIP deployment performance or efficiency improvement. Default CPU tiny measurements must be cheap; CUDA optional. Implement only necessary reusable measurement support and clear CLI/help/report fields, not a benchmarking framework. The full actual-backbone cost experiment remains later resource-dependent work.
5. CI: install a bounded pytest version compatible with current env and run tests/research plus the no-download CPU smoke (or a meaningful smoke test invoking the same public path) under GitHub Actions. Keep existing compile/lint/CLI checks; lint new source/tests. Do not require CUDA/private data/network model access. No production dependency expansion unless demonstrably necessary; test dependency in CI is expected. CI should remain within existing 30-minute timeout; aim local tiny smoke within a couple of minutes. Avoid CI double-running an expensive identical smoke without reason.
6. Meaningful RED/GREEN TDD: show missing/broken executable smoke and/or actual integration invariant failures before implementation, test reproducible outputs and no production-readiness promotion, role/mask/prediction consistency, invalid output path refusal and cleanup, timing semantics using controlled work where worthwhile. No prose/boilerplate tests, no implementation-mirroring tests, no timing performance thresholds. Do not weaken accepted tests. Run targeted tests then full research/lint/compile/smoke once stable; broaden/repeat only for edits, failures or unresolved concerns. Capture actual commands/results and non-scientific smoke/preflight/cost evidence for the orchestrator. Tests may use .cache task-owned --basetemp because /tmp/.git can make generic /tmp output invalid; do not disable path checks.

Keep changes focused: new smoke/preflight/cost module(s), package CLI registration, focused tests, existing CI. Substantial manuscript/operator prose is not assigned to this coding task; user-facing help and machine-readable reports are normal implementation. Report any genuine scope or protocol conflict rather than silently expanding it. Synthetic software acceptance remains independent of missing real-data resources. No real training, downloads, patient data, P5 or automatic locked-test unlock.

Env PYTHONPATH=src /home/tianqini/research/MMDC-CLIP-IVR/.venv/bin/python; Ruff same env. Normal sandbox often fails with bwrap loopback; use normal approved exec escalation and straightforward Python file edits or standard patch when apply_patch's nested helper fails. This is an authorized tool alternative, not permission to bypass an actual approval rejection. Never disable protections. No nested agents or model substitutions.

Current five-hour window begins 2026-09-21 14:40:54 UTC and ends 19:40:54 UTC (15:40 Toronto). Aim complete candidate within 60-75 minutes, preserving independent task review and Astra medium G4 gate time. Orchestrator commits immutable candidates and integrates/pushes. Final report exact scope/CLI examples, actual RED/GREEN/check commands/results, measured synthetic-only outputs, unmeasured real costs and readiness limitations, changed files and any dependency changes. No acceptance claim until fresh review.
